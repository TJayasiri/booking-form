[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback, works with deep links and refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
